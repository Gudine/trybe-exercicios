const randomNumberGenerator = () => Math.floor(Math.random() * 101);
const toUpper = (str) => str.toUpperCase();
const firstLetter = (str) => str[0];
const concat = (str1, str2) => str1 + str2;

export { randomNumberGenerator, toUpper, firstLetter, concat };
